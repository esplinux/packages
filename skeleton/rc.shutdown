#!/bin/sh

case "$1" in
reboot|poweroff)
  ;;
*)
  echo "Invalid action '$1' for rc.shutdown" 1>&2
  exit 1
  ;;
esac

/bin/killall5 -s TERM
/bin/sleep 3
/bin/killall5 -s KILL

/bin/sync
/bin/sleep 3

case "$1" in
reboot)
  /bin/reboot -f
  ;;
poweroff)
  /bin/halt -f
  ;;
esac

