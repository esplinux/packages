PACKAGE=espbuild
VERSION=HEAD
URL=https://github.com/esplinux/espbuild

include ../common/base.ninja

build ${PACKAGE}-${VERSION}/get.go: clone

build out: gobuild ${PACKAGE}-${VERSION}/get.go

build ${PACKAGE}-${VERSION}.tgz: package out

default ${PACKAGE}-${VERSION}.tgz
