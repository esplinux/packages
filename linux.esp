LINUX = struct(
  name    = "linux-headers", 
  version = "5.4.31", 
  rev     = "0"
)

SOURCE = source("https://cdn.kernel.org/pub/linux/kernel/v5.x/linux-%s.tar.xz" % LINUX.version)

make(SOURCE, "CC=clang HOSTCC=clang headers")

INCLUDE = SOURCE + "/usr/include"
